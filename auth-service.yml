spring:
  data:
    mongodb:
      database: ${MONGODB_DATABASE}
      uri: ${MONGODB_URI}

server:
  servlet:
    context-path: /uaa

ACCOUNT_SERVICE_PASSWORD: my-account-service-password
NOTIFICATION_SERVICE_PASSWORD: my-notification-service-password
STATISTICS_SERVICE_PASSWORD: my-statistics-service-password